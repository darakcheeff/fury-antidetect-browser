diff --git a/chrome/browser/bookmarks/BUILD.gn b/chrome/browser/bookmarks/BUILD.gn
index edd4475ea0..63d2b961b8 100644
--- a/chrome/browser/bookmarks/BUILD.gn
+++ b/chrome/browser/bookmarks/BUILD.gn
@@ -58,6 +58,7 @@ source_set("impl") {
   public_deps = [ "//chrome/browser:browser_public_dependencies" ]
 
   deps = [
+    "//components/fury",
     ":bookmarks",
     "//chrome/browser:browser_process",
     "//chrome/browser/commerce",
diff --git a/chrome/browser/bookmarks/bookmark_html_writer.cc b/chrome/browser/bookmarks/bookmark_html_writer.cc
index e5fb1fd803..3974083d3f 100644
--- a/chrome/browser/bookmarks/bookmark_html_writer.cc
+++ b/chrome/browser/bookmarks/bookmark_html_writer.cc
@@ -5,6 +5,9 @@
 
 #include "chrome/browser/bookmarks/bookmark_html_writer.h"
 
+#include "base/command_line.h"
+#include "components/fury/fury_switches.h"
+
 #include <stddef.h>
 #include <stdint.h>
 
@@ -655,6 +658,14 @@ namespace bookmark_html_writer {
 void WriteBookmarks(Profile* profile,
                     const base::FilePath& path,
                     BookmarksExportCallback callback) {
+  // Patch 0303. The single function both export callers reach — the settings UI
+  // and the bookmarkManagerPrivate extension API — so one refusal covers both.
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch(
+          fury::switches::kLockDataExport)) {
+    std::move(callback).Run(Result::kCouldNotCreateFile);
+    return;
+  }
+
   // We allow only one concurrent bookmark export operation per profile.
   if (profile->GetUserData(kBookmarkFaviconFetcherKey)) {
     return;
diff --git a/chrome/browser/extensions/BUILD.gn b/chrome/browser/extensions/BUILD.gn
index 1046137d6a..774494089a 100644
--- a/chrome/browser/extensions/BUILD.gn
+++ b/chrome/browser/extensions/BUILD.gn
@@ -583,6 +583,7 @@ source_set("extensions") {
     "//components/enterprise/browser/identifiers",
     "//components/enterprise/browser/promotion",
     "//components/favicon/content",
+    "//components/fury",
     "//components/guest_view/buildflags",
     "//components/guest_view/common",
     "//components/infobars/content",
diff --git a/chrome/browser/extensions/api/passwords_private/passwords_private_delegate_impl.cc b/chrome/browser/extensions/api/passwords_private/passwords_private_delegate_impl.cc
index dea7e79766..d81a575620 100644
--- a/chrome/browser/extensions/api/passwords_private/passwords_private_delegate_impl.cc
+++ b/chrome/browser/extensions/api/passwords_private/passwords_private_delegate_impl.cc
@@ -4,6 +4,9 @@
 
 #include "chrome/browser/extensions/api/passwords_private/passwords_private_delegate_impl.h"
 
+#include "base/command_line.h"
+#include "components/fury/fury_switches.h"
+
 #include <memory>
 #include <optional>
 #include <string>
@@ -579,6 +582,16 @@ void PasswordsPrivateDelegateImpl::RequestPlaintextPassword(
     int id,
     api::passwords_private::PlaintextReason reason,
     PlaintextPasswordCallback callback) {
+  // Patch 0303, and the reason blocking the CSV export alone is not enough.
+  // Reading credentials out one at a time is the same leak as exporting them in
+  // a batch, only slower, and this path is reachable from chrome://settings
+  // whether or not chrome://password-manager is disabled.
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch(
+          fury::switches::kLockDataExport)) {
+    std::move(callback).Run(std::nullopt);
+    return;
+  }
+
   AuthenticateUser(
       kPasswordManagerAuthValidity, GetReauthPurpose(reason),
       base::BindOnce(
diff --git a/chrome/browser/ui/passwords/settings/BUILD.gn b/chrome/browser/ui/passwords/settings/BUILD.gn
index a3c75c684f..9414423331 100644
--- a/chrome/browser/ui/passwords/settings/BUILD.gn
+++ b/chrome/browser/ui/passwords/settings/BUILD.gn
@@ -28,6 +28,7 @@ source_set("impl") {
     "password_import_controller.cc",
   ]
   deps = [
+    "//components/fury",
     ":settings",
     "//chrome/browser:primitives",
     "//chrome/browser/profiles:profile",
diff --git a/chrome/browser/ui/passwords/settings/password_export_controller.cc b/chrome/browser/ui/passwords/settings/password_export_controller.cc
index db12febfa3..ba5a4b901e 100644
--- a/chrome/browser/ui/passwords/settings/password_export_controller.cc
+++ b/chrome/browser/ui/passwords/settings/password_export_controller.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/command_line.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/path_service.h"
@@ -19,6 +20,7 @@
 #include "chrome/browser/ui/select_file_policy/chrome_select_file_policy.h"
 #include "chrome/common/chrome_paths.h"
 #include "chrome/grit/branded_strings.h"
+#include "components/fury/fury_switches.h"
 #include "components/password_manager/core/browser/export/password_manager_exporter.h"
 #include "components/password_manager/core/browser/ui/saved_passwords_presenter.h"
 #include "components/strings/grit/components_strings.h"
@@ -66,6 +68,19 @@ PasswordExportController::~PasswordExportController() {
 }
 
 bool PasswordExportController::Export(content::WebContents* web_contents) {
+  // Patch 0303. At the function that does the work, not at the button that
+  // starts it: the settings page reaches this over Mojo, and a message replayed
+  // by hand -- or an extension holding the passwordsPrivate permission --
+  // reaches it without any button at all. Hiding the button would demo
+  // perfectly and protect nothing.
+  //
+  // Lived in PasswordManagerPorter::Export until 153, where upstream split the
+  // porter into an export and an import controller.
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch(
+          fury::switches::kLockDataExport)) {
+    return false;
+  }
+
   if (exporter_ && exporter_->GetProgressStatus() ==
                        password_manager::ExportProgressStatus::kInProgress) {
     return false;
diff --git a/components/fury/fury_switches.cc b/components/fury/fury_switches.cc
index c42d598a2c..32ac0fc6d0 100644
--- a/components/fury/fury_switches.cc
+++ b/components/fury/fury_switches.cc
@@ -15,6 +15,7 @@ const char kFingerprintHandle[] = "fury-fp-handle";
 // so verify-0302.py launches with the Rust spelling and asserts the browser
 // refuses — a drifted name would otherwise fail silently and forever.
 const char kLockDevTools[] = "fury-lock-devtools";
+const char kLockDataExport[] = "fury-lock-data-export";
 
 }  // namespace switches
 }  // namespace fury
diff --git a/components/fury/fury_switches.h b/components/fury/fury_switches.h
index 11ff6d3386..9400652a6a 100644
--- a/components/fury/fury_switches.h
+++ b/components/fury/fury_switches.h
@@ -56,6 +56,15 @@ COMPONENT_EXPORT(FURY) extern const char kFingerprintHandle[];
 // machine.
 COMPONENT_EXPORT(FURY) extern const char kLockDevTools[];
 
+// Browser process only. Refuses the paths that take a profile's secrets out of
+// it in bulk: the password CSV export, the per-password plaintext reveal, and
+// the bookmark HTML export.
+//
+// Same channel and the same limits as kLockDevTools above. Not a claim about a
+// machine — a restriction on a person — so it does not travel in the persona
+// config that every renderer can see.
+COMPONENT_EXPORT(FURY) extern const char kLockDataExport[];
+
 }  // namespace switches
 }  // namespace fury
 
