# 08 — Конкуренты и позиционирование

Рынок плотный и зрелый. Открытому бесплатному проекту нужна не «такая же функциональность»,
а причина, по которой им будут пользоваться вместо готового платного продукта.

## Расстановка сил (2026)

| Продукт | Ядро | Позиция | Слабое место для нас |
|---|---|---|---|
| **Multilogin X** | Chromium (Mimic) + Firefox (Stealthfox) | Был верхним сегментом от €99/мес; с 07.2026 переставился в «облачные телефоны», вход срезан до $7.08/мес | Закрытость. Цена перестала быть их слабым местом — см. пересмотр 14.08.2026 |
| **Octo Browser** | Chromium | Топ-сегмент, сильный отпечаток, популярен в СНГ | Закрытость, цена |
| **Kameleo** | Chromium + Firefox + мобильные | Сильная автоматизация, мобильные профили | Цена, порог входа |
| **AdsPower** | Chromium | Лидер по командным функциям и no-code RPA, есть бесплатный тариф | Отпечаток слабее топа |
| **Dolphin Anty** | Chromium | Заточен под арбитраж/соцсети, сильное комьюнити | Отпечаток средний, узкая ниша |
| **GoLogin** | Chromium (Orbita) | Самый доступный, бесплатный тариф на 3 профиля, ~$24/мес | Отпечаток выше среднего, не топ |
| **Undetectable** | Chromium | Средний сегмент | — |
| **Camoufox** | Firefox, **open source** | Единственный сильный OSS. Библиотека для автоматизации, не продукт. С v149 везёт 312 готовых персон | Нет UI, нет команд, нет проектов, Firefox-ядро |
| **ShardX / ShardBrowser** | Chromium 152, **лончер MIT, ядро закрытое** | Ближайший аналог. Наш стек буквально: Tauri + React + Rust. Local API, MCP, SDK. 1018★, 236 персон (50 Android), синхронизатор окон — см. пересмотр 12.09.2026 | **Команд нет вообще.** Ядро — закрытый бинарник с их CDN, лицензия запрещает использовать его в сторонних продуктах |
| **Donut Browser** | движок Wayfern (закрытый), лончер **AGPL-3.0** | 3.8k звёзд, v0.30, ночные сборки, зрелый менеджер профилей. Прокси, WireGuard, self-hosted синхронизация, Local API, MCP | Ядро чужое и закрытое. Команд нет |
| **rustcloak** | MIT, ядро CloakBrowser (закрытое) | 2★, один коммит. Хорошая работа по CRX: разбор CRX3 и вписывание `manifest.key`, чтобы ID расширения пережил клонирование | Своей подмены нет вообще — весь антидетект это 10 `format!` в `build_args`. Отпечаток = одно `u64` в argv. Пароли прокси в открытом SQLite и в `ps`. Зашита устаревшая macOS-сборка движка (Chromium 145, 26 патчей против 58 на Linux) |
| **CloakBrowser** | MIT-обёртка, **бинарник проприетарный** | 31.4k★, замена Playwright в три строки, `humanize=True`, заявлен 71 патч C++ | Исходников движка нет: в репозитории 244 файла и ноль `.cc`/`.h`/`.patch`. Лицензия запрещает реверс, модификацию и распространение. Свежая версия по подписке. Движок ходит на сервер лицензий |
| **fingerprint-chromium** | **свой форк, патчи под BSD-3** | 3.0k звёзд. Патчи публикуются в тегах. Поддерживает 10 версий Chrome (132-148). **Тишина с 21.06.2026** | Нет WebGPU, нет экрана, нет модели персон. Конфиг через argv. Нет UI, нет команд. Похоже, остановился |
| **CloakBrowser-Manager** | MIT-менеджер (FastAPI + React + SQLite), ядро CloakBrowser закрытое | 933★, появился летом 2026. Описание — дословно «free, self-hosted Multilogin alternative». Docker и нативные сборки, CDP на профиль, таймзона из выходного IP, гуманизация | Один `AUTH_TOKEN` на установку, **команд нет**. Ядро по лицензионному ключу, бесплатно один одновременный профиль |
| **Linken Sphere 2** | Chromium | Верхний сегмент, СНГ. Прогрев с глубиной и выбором поисковика, массовая раскладка cookies по сессиям, чекер чистоты прокси | Закрытость, цена. Смотрен по обзорам, не по докам |
| **Nstbrowser** | Chromium | Бесплатно 500 профилей / 30 запусков в день, RPA с маркетплейсом, облачные контейнеры. Продаёт «Cloudflare Bypass» отдельным продуктом | Это категория обхода антиботов как услуги — за границей [ACCEPTABLE_USE](../ACCEPTABLE_USE.md) |
| **Incogniton, Hidemyacc, MoreLogin, BitBrowser** | Chromium (BitBrowser + Firefox) | Средний и нижний сегмент. У всех четырёх — синхронизатор окон; Incogniton — «вставить как ввод человека» | Смотрены по обзорам, не по докам |

## ShardX — разбор ближайшего аналога

Изучен 30.07.2026. Самый близкий к Fury существующий проект, и разбирать его
надо внимательно, потому что он и подтверждает нишу, и показывает развилку.

### Как он устроен

**Лончер под MIT** — Tauri + React + Rust, то есть в точности наш стек
из [01](01-architecture.md). Desktop UI, локальный HTTP API, MCP-сервер,
SDK для Python/Node/Rust, 170 профилей устройств в комплекте.

**Ядро — закрытый бинарник**, патченный Chromium 149, который лончер скачивает
с их CDN при первом запуске. Исходников нет ни в репозитории, ни где-либо ещё.
Лицензия ядра прямо запрещает:

- реверс-инжиниринг, дизассемблирование, попытки восстановить исходник;
- распространение изменённой версии ядра;
- использование ядра «как части коммерческого антидетект-браузера или сервиса».

### Что это значит для нас

**Их ядро использовать нельзя.** Не только из-за лицензии. Его нельзя собрать,
нельзя проверить, нельзя пропатчить и нельзя ребейзить — а зависимость ключевого
компонента от чужого закрытого бинарника с чужого CDN прямо противоречит тому,
что Fury продаёт: самохостингу и проверяемости. Их релизный цикл стал бы нашим.

**Их лончер изучать и переиспользовать можно** — MIT это разрешает. Это
легально экономит недели на [фазе 4](09-roadmap.md), и стек совпадает буквально.

**Их список пропатченного — готовый валидированный чеклист.** Семь векторов,
которых не было в [02](02-fingerprint-surface.md), уже добавлены оттуда:
`x-client-data`, WebAuthn platform authenticator, `color-gamut`/HDR,
стабильность GREASE, ограничение максимального размера окна, WebGPU при
заявленном Linux. Чужой changelog оказался полезнее собственного брейншторма.

**Widevine они решили** — в списке «Widevine pre-warmed». Значит проблема
из [10](10-legal-licensing.md) обходится, а не блокирует.

### Главное: команд у них нет

В документации **ни одного упоминания** команд, многопользовательского доступа,
проектов, выдачи прав и общих рабочих пространств. «Workspace» у них — про
организацию профилей внутри одной локальной установки.

То есть **сильнейший бесплатный конкурент не закрывает ровно ту нишу, под
которую мы строим весь продукт**: организации, проекты, RBAC, синхронизация,
локи, аудит. Это лучшее подтверждение позиционирования, какое можно было
получить.

### Их результаты тестов — маркетинг, пока не проверены

В README заявлены `fingerprint.com` без детекта, `browserscan.net` 100%
authenticity, reCAPTCHA score 0.9. Это самоотчёт. У нас есть
[detect-suite](07-detection-baseline.md) и снятый эталон Chrome 150 — можно
проверить объективно и получить первую честную оценку того, чего вообще
достигает грамотно пропатченное ядро.

**Оговорка по безопасности:** проверка требует скачать и запустить закрытый
бинарник от анонимной команды в браузере, который потом держит боевые аккаунты.
Это осознанное решение о доверии, а не рутинный шаг. Разумно — на отдельной
машине или в виртуалке, не там, где лежат рабочие профили.

### Отставание версий — тот самый ребейз

Описание репозитория говорит Chromium 148, README — 149, актуальный stable — 150.
То есть отставание на один-два мажора, и **про ребейз в их документации нет ни
слова**. Это ровно [проблема из 03](03-chromium-fork.md), и она видна у проекта,
который делает всё остальное хорошо.

### Развилка, которую они показывают

ShardX доказывает жизнеспособность модели **«открытый лончер + закрытое ядро»**.
Это реальный запасной вариант для Fury, если поддержка открытого ядра окажется
неподъёмной: продуктовый слой остаётся открытым и проверяемым, ядро —
собственным бинарником. Хуже для доверия, дешевле в поддержке. Решать не сейчас,
но вариант зафиксирован.

## Donut Browser — зрелый лончер, чужое ядро

3.5k звёзд, 369 форков, **1572 коммита**, AGPL-3.0, версия 0.28.2. Активно
развивается. Стек снова наш: Tauri + Next.js + Rust.

**Ядро — Wayfern**, «privacy-focused Chromium fork» со спуфингом на уровне C++
и собственными CDP-командами. Открыт только MIT-хелпер поверх Playwright;
про лицензию и доступность исходников самого движка ни на GitHub, ни на
wayfern.com **не сказано ничего**. То есть та же схема, что у ShardX: открытый
лончер, закрытое ядро.

**Что у них есть, а у нас в планах:** прокси HTTP/HTTPS/SOCKS4/SOCKS5 на профиль
с динамическими URL, WireGuard на профиль, DNS-блокировщик, группы профилей,
импорт профилей из Chrome/Edge/Brave, Local API, MCP-сервер, роль браузера по
умолчанию с маршрутизацией ссылок по профилям, и — **self-hosted синхронизация
с E2E-шифрованием**.

Последнее пересекается с [06](06-profile-sync.md) напрямую. Их лончер под
**AGPL-3.0** — та же лицензия, что мы выбрали, значит его код можно легально
читать и переиспользовать. 1572 коммита зрелого Tauri-менеджера профилей — это
большая часть [фазы 4](09-roadmap.md), уже написанная и отлаженная.

**Команд у них тоже нет.** Ни слова про многопользовательский доступ, выдачу
прав, общие проекты. Синхронизация — между устройствами **одного** пользователя,
не между людьми. Ниша Fury снова подтверждается, теперь на третьем проекте
подряд.

## fingerprint-chromium — единственный открытый движок, и это меняет план

[adryfish/fingerprint-chromium](https://github.com/adryfish/fingerprint-chromium),
2.9k звёзд, 420 форков, **BSD-3-Clause**, на базе ungoogled-chromium.

**Патчи опубликованы** — в тегах репозитория, а не в main, с задержкой примерно
месяц после бинарных релизов (защита от перепаковки). Поддерживаются десять
версий: Chrome 132, 133, 134, 135, 136, 138, 139, 142, 144, 148. Это не заявление
о намерениях, а доказанная история ребейзов — ровно та работа, которую
[03](03-chromium-fork.md) называет главным обязательством проекта.

Пропатчено: navigator и User-Agent, ОС, audio, плагины, число ядер и память,
WebGL (картинка и метаданные), шрифты, canvas (картинка и текст), ClientRects,
WebRTC, язык, таймзона. Конфигурация — флаги командной строки:
`--fingerprint=<seed>`, `--fingerprint-platform`, `--fingerprint-brand`.

### Чего у них нет — и это ровно наша дифференциация

| Вектор | fingerprint-chromium |
|---|---|
| **WebGPU** | **нет** |
| Разрешение и геометрия экрана | **нет** |
| Модель персон (связность значений между собой) | **нет** |
| Client Hints как единый источник правды | не заявлено |
| Согласованность Permissions API | не заявлено |
| speechSynthesis, mediaDevices, ширина скроллбара | не заявлено |
| Согласованность между контекстами исполнения | не заявлено |

TLS/JA3 и HTTP/2 из этой таблицы убраны, и это находка, а не сокращение. Раньше
здесь стояло «не закрыт слой 1 (сеть), а именно он делает подмену бесполезной».
Сверено с исходниками M150 31.07.2026: Chrome вызывает
`SSL_set_permute_extensions(ssl_, 1)` и `SSL_CTX_set_grease_enabled(ctx_, 1)`, а
`SSL_CTX_set_cipher_list` не вызывает вовсе — порядок расширений меняется на каждое
соединение, стабильного JA3 у настоящего Chrome нет. Любой форк Chromium, не
трогающий `net/socket` и boringssl, отдаёт неотличимый ClientHello: и они, и мы.
Закрепить JA3 за профилем значило бы стать МЕНЕЕ похожим на Chrome, поэтому
0200/0210/0220 сняты с плана и у нас (`core/patches/series`). Дифференциацией это
быть перестало.

То есть закрыт слой 2 из [02](02-fingerprint-surface.md) — наполовину, и это показало
измерение, а не их README.

### Разбор исходников — проведён 31.07.2026

Клонированы теги и прочитаны патчи. **Тег 148 содержит только README** —
задержка публикации подтвердилась. Последний тег с исходниками — **144**.

Патчи отпечатка: **16 файлов, 2547 строк, 38 файлов Chromium**. Остальные ~112
патчей в репозитории — база ungoogled-chromium, inox, bromite, iridium.

#### Механизм доставки конфига

`000-add-fingerprint-switches.patch` заводит ключи командной строки
(`--fingerprint`, `--fingerprint-platform`, `--fingerprint-screen-width`, …) и
прокидывает их в процессы рендерера через `render_process_host_impl.cc`.
В рендерере значения читаются напрямую:

```cpp
const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
if (command_line->HasSwitch(switches::kFingerprintHardwareConcurrency)) { … }
```

**Из этого следует важное: воркеры покрыты автоматически.** `CommandLine`
процесса читается из любого контекста внутри него, включая dedicated worker,
shared worker и service worker. Никакого mojo-плюмбинга не нужно. Решение
элегантное — и именно поэтому у них нет отдельного патча `0001`, как у нас.

#### Где патчи размещены — грамотно

- `core/execution_context/navigator_base.cc` — общая база `Navigator` и
  `WorkerNavigator`, то есть UA правится один раз для всех контекстов;
- `platform/fonts/font_cache.cc` — **уровень fallback**, а не перечисления.
  Ровно то, что [02](02-fingerprint-surface.md) называет единственным рабочим
  способом фильтрации шрифтов;
- `canvas2d/base_rendering_context_2d.cc` и `platform/graphics/` — общие с
  `OffscreenCanvas`, значит canvas в воркере закрыт;
- `v8/src/inspector/v8-runtime-agent-impl.cc` — защита от детекта через
  `Runtime.enable`. Тонкая вещь, мало кто делает.

#### Два серьёзных изъяна

**1. Конфиг в аргументах процесса.** `ps` показывает весь отпечаток профиля
любому процессу в системе. Мы от этого ушли осознанно, передавая конфиг через
pipe ([01](01-architecture.md), [launcher.rs](../agent/src/launcher.rs)).
Копировать нельзя.

**2. Нет модели устройства — значения выводятся из seed по отдельности.**

```cpp
// hardwareConcurrency
return static_cast<unsigned>(((fingerprint % 13) + 4) * 2);   // 8,10,…,32
```

```cpp
// deviceMemory
return 8;                                                      // ВСЕГДА 8
```

`deviceMemory` **захардкожен**, а число ядер размазано равномерно по 13 корзинам.
В природе распределение ядер сосредоточено на 4, 8, 12, 16 — равномерный разброс
до 32 сам по себе аномален в массе. И связи между значениями нет: 32 ядра при
8 ГБ памяти — допустимая для них комбинация.

Базы реальных персон в репозитории нет вообще, связности между значениями тоже.

А прежнее «наш валидатор отверг бы их собственный вывод» — неправда, и это
проверено по коду, а не рассуждением. Единственное правило, связывающее ядра с
памятью, — `device_memory_gb < 4 && hardware_concurrency > 8`
([fingerprint.rs:249](../shared-rs/src/fingerprint.rs)), а у них память всегда 8 ГБ.
32 ядра при 8 ГБ проходят наш `validate()` насквозь; правило ловит другой
случай — 2 ГБ и 24 ядра, ровно его берёт тест `impossible_hardware_is_rejected`.
Их вывод ловит не валидатор, а каталог персон: значения берутся с реальной
машины целиком, а не выводятся из seed по отдельности. Это ровно та ошибка,
против которой построена вся модель персон в [02](02-fingerprint-surface.md):
валидный, но небывалый набор хуже отсутствия подмены — и правило под этот
конкретный перекос у нас пока не написано.

#### Чего нет — проверено поиском по всем патчам

| Вектор | |
|---|---|
| `net/socket`, boringssl — TLS/JA3 | отсутствует |
| `net/spdy` — HTTP/2 | отсутствует |
| `modules/webgpu` | отсутствует |
| `core/frame/screen.cc` — разрешение экрана | отсутствует |
| `modules/permissions` | отсутствует |
| `modules/speech` — голоса | отсутствует |
| `modules/mediastream` — устройства | отсутствует |
| `navigator_language` | отсутствует |

Audio закрыт только в `offline_audio_context.cc` — живой `AudioContext`
и `AnalyserNode` не тронуты.

#### Итоговая оценка — ниже, чем я предполагал

Раньше в этом документе стояло «покрыто примерно две трети нужного».
**Измерение это не подтвердило.** Реально закрыт **JS-слой наполовину**.

Прежняя вторая половина оценки — «сетевой слой на ноль» — снята: сетевых патчей
нет и у нас, и это не минус ни одному из нас. Chromium-форк, не трогающий
`net/socket` и boringssl, отдаёт ClientHello настоящего Chrome по построению
(`core/patches/series`, блок network; в `core/patches/` нет ни одного файла 02xx).
Старая цифра «12 из 30 векторов» считала TLS и HTTP/2 незакрытыми векторами и
вместе с ними убрана — новой нет, пока никто не пересчитал.

Брать за основу всё равно стоит: 2547 строк рабочего кода под BSD-3, грамотно
размещённых, с доказанной историей ребейзов на десять версий — это экономит
недели. Но переделать придётся доставку конфига и добавить модель персон,
а WebGPU и экран писать с нуля.

### Что это значит для стратегии

Появляется третий путь по ядру, которого не было в первоначальном разборе:

**Взять их BSD-3 патч-серию за основу и дописать то, чего нет.** Не «готовый
слой 2», как я предполагал до разбора, а половина слоя 2 — но половина рабочая,
грамотно размещённая и переживающая ребейзы. BSD-3 совместим с AGPL, юридически
чисто. Экономия реальная, но недели, а не месяцы.

Риски, которые надо принять осознанно:

- **Задержка публикации в месяц.** Их патчи под новую версию появляются позже
  бинарников. Ребейзить придётся самим, а не ждать.
- **Chrome 148 против нашего 150.** Отставание на два мажора уже в точке старта.
- **Конфиг через argv.** Аргументы процесса читаются любым другим процессом
  в системе — ровно то, чего мы избегаем в
  [launcher.rs](../agent/src/launcher.rs), передавая конфиг через pipe. Это
  копировать нельзя, надо переделать.
- **Покрытие контекстов исполнения неизвестно.** Работает ли их спуфинг
  в Worker и cross-origin iframe — не заявлено. Наш
  [detect-suite](07-detection-baseline.md) отвечает на это за две минуты, и
  ответ определяет, годится ли их серия за основу вообще.

Проверять надо измерением, а не чтением README. У них есть бинарные релизы под
macOS — но это снова запуск чужого бинарника, осознанное решение о доверии.
В их пользу то, что исходники они всё-таки публикуют, в отличие от Wayfern
и ShardX.

## Пересмотр рынка — 14.08.2026

Десять продуктов прочитаны заново по официальным страницам, докам и репозиториям.
Ниша подтвердилась в пятый раз, но четыре факта изменились, и один из них
отменяет утверждение, стоявшее в этом файле как наше преимущество.

**Оговорка о методе, и она здесь важнее обычного.** Всё в этом разделе —
чтение README, прайсов и документации, то есть **заявления вендоров**, а не
измерения. Ровно так же читался AdsPower, у которого «canvas: защищён» в
интерфейсе и байт в байт чистый Chrome в замере. Ни одну строку ниже нельзя
переносить в план как факт о качестве; это факты о том, **что продаётся**.

### 1. MCP перестал быть преимуществом

В этом файле и в [16](16-parity-and-beyond.md) MCP-сервер числился нашей сильной
стороной. За год его завели **AdsPower, GoLogin, Donut Browser и ShardBrowser** —
у GoLogin он вообще лежит открытым исходником (`gologinapp/gologin-mcp`). То есть
это паритет, причём догнанный не нами.

Практического долга здесь нет: `tools/mcp/fury-mcp.py` написан и в CI. Долг
риторический — перестать называть это отличием.

### 2. Автоматизация — самое большое отставание, и оно не про RPA

Здесь легко ошибиться, потому что громче всего продаётся визуальный конструктор
сценариев (AdsPower RPA + маркетплейс шаблонов, Dolphin Scenario Builder), а он
у нас сознательно отложен ([12](12-ui-reference.md)) и отложен верно: это
отдельный большой продукт.

Но рядом с ним стоят **две функции, которых нет ни у одного OSS-проекта и есть
у обоих лидеров по эргономике**, и они дешевле конструктора на порядок:

**Синхронизатор окон.** Действия в одном окне зеркалируются на N профилей с
рандомизированными задержками — AdsPower Multi-Window Synchronizer, Dolphin
Synchronizer. Это 80% пользы RPA за малую долю его сложности, и главный
операционный сценарий агентства — прогрев пачки аккаунтов — закрывается именно
им, а не конструктором. У нас нет ничего: поиск по `desktop/src` и `agent/src`
не находит ни одной сущности.

**Прогрев cookies.** Octo Cookie Robot обходит список URL headless и собирает
трекинг-пиксели, отбраковывая мёртвые ссылки; Dolphin планирует «реалистичные
сессии». У нас есть импорт и экспорт cookies (`agent/src/cookies.rs`,
`Cookies.tsx`), но прогрева нет. И это **вектор отпечатка, а не удобство**:
профиль без истории посещений сам по себе аномален, сколько бы правильных
значений ни отдавал `navigator`.

### 3. Camoufox решил проблему каталога персон статистикой

Это самое ценное, что дал пересмотр, и оно бьёт прямо в задачу 3.4
([16](16-parity-and-beyond.md)), где записано, что 170 персон упираются в доступ
к 170 настоящим машинам.

Camoufox с v149 везёт **312 персон**, и берёт их не с машин, а из
**BrowserForge** — генерации по реальным статистическим распределениям
заголовков и свойств. То есть третий путь между «измерить машину» и «сочинить
комбинацию», которого в нашем разборе не было.

Принимать его на веру нельзя: наш собственный довод против сочинённых персон
(«валидный, но небывалый набор хуже отсутствия подмены») никуда не делся, а
распределение может быть верным помаржинально и давать невозможные сочетания.
Но проверяется это дёшево — прогнать их распределения через наш `validate()` и
посмотреть, сколько выживает. Если выживает много, 3.4 перестаёт упираться в
железо.

### 4. Безопасность аккаунта команды — раздел, которого у нас нет

AdsPower и GoLogin продают ровно то, что покупает агентство: 2FA на рабочее
пространство, allowlist IP для входа, оповещение о входе с нового устройства,
удалённое завершение чужих сессий, отслеживание устройств. Поиск по `server/src`
не находит ни одного из этих понятий — у нас opaque-токены и Argon2id, и всё.

Для продукта, чья ниша — команды, это ближе к причине покупки, чем ещё один
вектор отпечатка.

### 5. Что изменилось в расстановке

- **Multilogin** переставился в «облачные телефоны» (настоящее Android-железо
  с ADB-доступом) и срезал вход до $7.08/мес. «Дорогой верхний сегмент» как
  описание их слабого места устарело.
- **ShardBrowser** — 641★ против 593 при прошлом разборе, ядро по-прежнему
  закрытый бинарник, релизы **не подписаны** (нужен обход Gatekeeper и
  SmartScreen). То есть в дистрибуции они не настолько впереди, как казалось.
- **Donut Browser** — v0.29.3, очень активный цикл, и раздача шире всех:
  DMG + Homebrew, Windows installer, deb/rpm/AppImage, Nix.
- **Camoufox** пережил около года паузы в поддержке по личным обстоятельствам
  мейнтейнера, база Firefox успела состариться, сейчас разработка возобновлена.
  Это напоминание, чего стоит обязательство по ребейзам из
  [03](03-chromium-fork.md), на живом примере сильнейшего OSS в области.

### 6. Мобильные: развилка, которую видно только теперь

Облачные телефоны есть у троих (Multilogin, GoLogin Cloud Android, AdsPower
Cloud Phone) и требуют парка машин — отказ из этого файла в силе.

Но **Octo даёт мобильные отпечатки iOS и Android без всякого облака**, а Dolphin
до сих пор PC-only и обещает мобильные «скоро». То есть мобильный отпечаток и
облачный телефон — разные продукты, и первый достижим той же моделью персон, что
уже написана. В прошлой редакции они были свалены в один отказ «мобильные
отпечатки в v1», и это была ошибка категории.

### 7. Ниша — подтверждение в пятый раз

Ни Camoufox, ни Donut, ни ShardBrowser, ни новые мелкие проекты
(OpenIncognito/Antic-Browser на Flutter+Python, `brodev3/antidetect`) не имеют
многопользовательского режима. Потолок зрелого OSS сегодня — self-hosted
синхронизация Donut, и она между устройствами **одного** человека.

### 8. Что увидел в нас человек со стороны

Побочный результат прогона, и полезный: наш репозиторий попался в выдаче и был
разобран как чужой проект. Внешнее чтение только README дало: «нет релизов,
тонкий инструментарий профилей, **UI регистрации участников не построен**, персон
26, MCP и SDK нет».

Из этого списка верны только релизы. `Users.tsx` — 372 строки с приглашением,
ролью и исключением; `tools/mcp/fury-mcp.py` — шесть инструментов; импорт-экспорт
cookies написан. То есть **сделанное не читается снаружи**, и это дешевле любой
задачи из плана: правится README, а не кодом.

## Пересмотр рынка — 12.09.2026

Четыре недели после прошлого захода. За это время у нас закрылась фаза 0:
теги `v0.1.0`–`v0.1.2`, macOS `.dmg` и Windows-инсталлятор, ядро на 153, Widevine
на обеих платформах. То есть впервые сравнение идёт между **скачиваемыми**
продуктами, и прошлый вывод «мы не участвуем в сравнении функций» снят.

Прочитаны официальные release notes AdsPower, Multilogin, GoLogin, Kameleo,
Undetectable, блог Dolphin, README и релизы ShardBrowser, Donut, Camoufox,
fingerprint-chromium и **CloakBrowser-Manager** — новый проект, которого в
прошлом разборе не было. Впервые посмотрены Linken Sphere, Nstbrowser,
Incogniton, Hidemyacc, MoreLogin, BitBrowser — по обзорам, не по докам, и
это слабее.

**Та же оговорка, что и месяц назад: всё ниже — заявления вендоров.** Ни одна
строка не измерена detect-suite'ом. AdsPower, у которого «canvas защищён» в
интерфейсе и байт в байт чистый Chrome в замере, — напоминание, зачем эта
оговорка стоит в каждом пересмотре.

### 1. Ядра: мы впереди по версии, и это первый раз

| Продукт | Ядро на 12.09.2026 |
|---|---|
| **Fury** | **Chrome 153** (пин от 10.09) |
| AdsPower, Multilogin, Undetectable, ShardBrowser, Camoufox | 152 |
| GoLogin (Android) | 148 |
| fingerprint-chromium | 148, последний коммит 21.06 — **три месяца тишины** |
| Dolphin Anty | **144** |

Месяц назад мы отставали на три мажора и это само по себе было отпечатком
([03](03-chromium-fork.md)). Теперь на мажор впереди рынка. Удерживать это —
и есть то обязательство по ребейзам, ради которого написан 03; Dolphin на 144
показывает, как выглядит продукт, который его не тянет.

fingerprint-chromium — единственный открытый движок с патчами — похоже,
остановился. Если так, третий путь по ядру из разбора 31.07 («взять их серию
за основу») закрылся сам: брать нечего, наша серия свежее.

### 2. Синхронизатор окон появился у OSS — и в интересной конструкции

Месяц назад: «нет ни у одного OSS-проекта». Уже неверно. **ShardBrowser
выпустил синхронизированные окна**, и описание конструкции стоит прочитать
целиком, потому что оно умнее, чем у AdsPower и Dolphin:

- **лидера нет** — ведёт то окно, в котором сейчас курсор, переключил окно —
  переключил ведущего;
- каждое окно исполняет действие **своим моторным профилем**, а не
  воспроизводит запись — два окна не двигаются одинаково;
- **заполнение форм намеренно не зеркалируется**: у каждого окна своя
  сгенерированная персона, и зеркалить ввод значило бы вписать ответы одного
  профиля в форму другого.

Третий пункт — правильное решение, до которого мы бы дошли только на грабле.
Задача 5.1 в [16](16-parity-and-beyond.md) теперь имеет референс лучше
коммерческого, и лончер ShardX под MIT — читать можно.

### 3. CloakBrowser-Manager занял нашу формулировку

Новый проект от авторов CloakBrowser: **933★, MIT, Docker и нативные сборки,
FastAPI + React + SQLite**. Описание репозитория — дословно: «Free, self-hosted
Multilogin alternative». README продаёт то же, что наш [08](#где-реально-можно-быть-лучше-платных):
«каждый аккаунт, каждая cookie лежат на чужом сервере» — и предлагает
самохостинг как ответ.

Что у них есть, чего нет у нас: таймзона и локаль **следуют за выходным IP
прокси автоматически** (у нас `proxies.check` записывает `timezone` выхода в
`last_timezone`, но в профиль не подставляет); гуманизация мыши, клавиатуры и
скролла как опция профиля; статус лицензии и «здоровье шрифтов Windows» в
шапке.

Чего нет: **многопользовательского режима**. Защита API — один `AUTH_TOKEN` на
всю установку. Ядро закрытое, ходит на сервер лицензий, бесплатно — один
одновременный профиль, дальше по подписке.

Для нас это значит две вещи. Слово «self-hosted» перестало быть нашим:
теперь его надо уточнять — **self-hosted с открытым ядром и командой**, потому
что просто «self-hosted» уже стоит у проекта с 31k★ за спиной. И ниша
подтверждается **в шестой раз**: даже проект, который пришёл ровно на нашу
позицию, сделал single-user.

### 4. «AI-агент» — новая строка в каждом прайсе

AdsPower запустил AI Agent в августе (задачи на естественном языке: создание
профилей, публикация контента), в сентябре — кредиты и выбор модели.
Nstbrowser показывает «AI Agent Skills». Multilogin — ClawHub для ADB-автоматики
облачных телефонов.

Это **не** пробел у нас, но требует записать различие явно. У них — их LLM на их
сервере, за их кредиты, с доступом к профилям пользователя. У нас — `fury-mcp.py`,
который отдаёт те же шесть операций **любому** агенту пользователя: Claude
Desktop, Cursor, свой скрипт. Ключ и модель — пользователя, данные профиля
сервер вендора не видит. Для проекта, чей аргумент — «cookies боевых аккаунтов
не на чужом сервере», hosted-агент был бы противоречием.

Долг тут, как и с MCP месяц назад, риторический: в README нет ни строки о том,
что MCP — это и есть «AI-агент», только без посредника. Это 5.11.

### 5. QUIC подтверждён вторым вендором

Undetectable 2.50 (01.09): SOCKS5 с UDP и **QUIC/HTTP-3**. ShardBrowser везёт
QUIC через свой UDP-релей с июля. Два независимых вендора сочли расхождение
достаточно видимым, чтобы делать. У нас `--disable-quic` в `launcher.rs`, и
это видно со стороны сервера — 5.13 остаётся, довод стал сильнее.

### 6. `x-browser-validation` — соседний вектор с 5.12

Undetectable 2.47 пишет «улучшена генерация заголовка `x-browser-validation`».
Это не `x-client-data`: Chrome с ~M130 шлёт на гугловые домены четыре
заголовка `X-Browser-Validation`, `X-Browser-Channel`, `X-Browser-Copyright`,
`X-Browser-Year`, и первый — хеш от API-ключа сборки и User-Agent. Сборка без
гугловых ключей либо не шлёт его вовсе, либо шлёт с хешем от **своего** UA,
а страница видит подменённый.

Мерить дёшево: один запрос к `accounts.google.com` через релей с логом
заголовков. Ответ либо закрывает 5.12 вместе с этим («никто не проверяет»),
либо добавляет к 5.12 второй заголовок. До замера — не задача.

### 7. Мобильные без облака — уже пять вендоров и один OSS

Месяц назад это делал один Octo. Теперь: Multilogin (мобильные профили в
десктопном браузере — touch-события, Android-шрифты, эмуляция 3G/4G/5G),
Undetectable (Android-эмуляция с сетевыми настройками), AdsPower (UA Android 16),
Octo — и **ShardBrowser с 50 Android-персонами из 236**. Dolphin по-прежнему
PC-only.

Довода **из нашего продукта** всё ещё нет, и 5.14 остаётся «решать отдельно».
Но рынок проголосовал заметнее, и один факт стоит записать: ShardX делает это
той же моделью персон, что и десктоп, без отдельного ядра. То есть оценка
«отдельное ядро, отдельный объём» из «Чего сознательно не делаем» может быть
завышена. Проверяется чтением их патчей, не догадкой.

### 8. Kameleo Fingerprint Inspector — то, что у нас уже написано, но не в приложении

Kameleo 5.1 (03.08): «Fingerprint Inspector как опция запуска профиля» —
инструментирование в реальном времени того, что страница читает из отпечатка.

У нас это есть целиком — `tools/detect-suite/probe.html` плюс `collector.py`,
плюс 27 verify-скриптов — и **ничего из этого не достижимо из приложения**.
Пользователь, которому отпечаток важнее всего, не может нажать кнопку «покажи,
что видит сайт в этом профиле». Механика готова: `profile.launch` с
`start_url` на локальный probe. Это самая дешёвая строка пересмотра, и она
превращает нашу культуру измерений из внутренней в видимую.

### 9. Ограничение сайтов по группе участников — командная функция на нашем релее

AdsPower (сентябрь): ограничения доступа к «чувствительным сайтам» задаются
на **группу участников**, а не на всю команду. Это командная функция — наша
ниша — и у нас есть механизм: `agent/src/blocklist.rs` отказывает на уровне
релея, который DoH не обходит. Не хватает привязки списка не к профилю, а к
гранту или роли. Ближе к 5.3 (безопасность команды), чем к паритету по UI.

### 10. Мелочи, которые у большинства есть — сверено с кодом

| | у нас | у кого |
|---|---|---|
| Восстановление вкладок при открытии | есть (`--restore-last-session`, `launcher.rs:126`) | Dolphin — только что добавил |
| Корзина с восстановлением | есть (`Trash.tsx`, `profiles.restore`) | Dolphin ускорил |
| Заметки к профилю | есть | Multilogin расширил до 1500 знаков |
| Избранные профили | нет | Multilogin |
| Имя профиля в заголовке окна браузера | **не проверено** | Dolphin |
| Массовое обновление отпечатков без открытия профилей | нет — `profiles.upsert` по одному | Dolphin |
| Диагностика сети: прокси упал или мешает системный VPN | нет — `proxies.check` отвечает «да/нет» | AdsPower, Kameleo («объяснение простым языком») |
| Таймзона/локаль из выходного IP автоматически | **есть**, при каждом запуске (`ipc.rs:1535`) — в первой редакции этого раздела стояло «наполовину», по устаревшему комментарию | CloakBrowser-Manager, ShardX |
| «Вставить как ввод человека» | нет | Incogniton |
| Прогрев с глубиной, числом URL, временем на URL, выбором поисковика | нет (5.2) | Linken Sphere, Octo |
| Массовая загрузка cookies с раскладкой по профилям | нет — импорт по одному | Linken Sphere |
| Docker / headless режим без UI | нет, Linux — не цель ([README](../README.ru.md)) | Multilogin, Kameleo, CloakBrowser-Manager |
| Блокировка Local Network Access | по построению: весь трафик уходит в релей и дальше в апстрим-прокси, до LAN не доходит. **Не измерено** | Dolphin — как функция |

### 11. Что изменилось в расстановке

- **ShardBrowser** — 1018★ (было 641), Chromium 152, **236 персон** (36 mac /
  131 win / 19 linux / 50 android; было 170), синхронизатор окон, Widevine
  через component updater — тот же способ, к которому мы пришли 10.09.
  Команд нет.
- **CloakBrowser** — 31.4k★ (было 29.6k); появился Manager, см. п. 3.
- **Donut** — 3819★ (было 3557), v0.30.0, ночные сборки каждый день,
  проверка контрольной суммы Wayfern при загрузке. Команд нет.
- **Camoufox** — 11.8k★, v152 beta.30, разработка ожила: гуманизированная
  траектория мыши восстановлена, починена связность WebGL↔экран, утечка
  голосов хоста. Firefox, библиотека.
- **fingerprint-chromium** — 3029★, тишина с 21.06.
- **Multilogin** — бесплатный тариф на 5 облачных профилей с 15.07, ядро
  каждые 3-4 недели (149→150→151→152 за лето). Это темп, с которым надо
  сравнивать наш ребейз.
- **Dolphin** — бесплатный тариф срезан с 10 до 5 профилей; ядро 144.
- **Nstbrowser** — бесплатно 500 профилей и 30 запусков в день; RPA с
  маркетплейсом; заявлен Cloudflare Bypass как продукт. Это уже другая
  категория — обход антиботов как услуга, ровно то, что
  [ACCEPTABLE_USE](../ACCEPTABLE_USE.md) выводит за границу проекта.

### 12. Ниша — подтверждение в шестой раз, но формулировку надо сузить

Ни один из проверенных, включая новый CloakBrowser-Manager, не имеет
многопользовательского режима. Но «self-hosted» как слово теперь занято
проектом с 31k★ и закрытым ядром. Наша позиция должна звучать точнее:
**открытое ядро, которое можно собрать и проверить, плюс команда с ролями,
локами и аудитом, плюс self-hosted**. Любые два из трёх у кого-то есть. Все
три — ни у кого.

### Что из этого брать в работу

По убыванию отношения пользы к труду, и только то, где есть довод из
нашего продукта:

1. **Probe из приложения** (п. 8) — механика написана, нужна кнопка. Дни.
2. ~~Таймзона из выходного IP~~ — уже сделано при запуске, см. таблицу.
3. **README про MCP как AI-агент без посредника и про self-hosted с открытым
   ядром** (п. 4, 12) — это 5.11, и она подорожала: теперь надо ещё и
   отстроиться от CloakBrowser-Manager. День.
4. **Замер `x-browser-validation`** (п. 6) — один запрос через релей. Час.
   Решает судьбу 5.12.
5. **Синхронизатор окон** (п. 2) — 5.1, с конструкцией ShardX как референсом:
   без лидера, без зеркалирования форм.
6. **Ограничение сайтов по гранту** (п. 9) — на существующем `blocklist.rs`,
   в рамках 5.3.

Не брать без отдельного решения: мобильные персоны (п. 7), Docker/headless
(Linux не цель), «вставить как человек» (часть 3.5, а 3.5 начинается
с измерения).

## Ключевое наблюдение

**Открытой ниши «OSS + своё Chromium-ядро + командная работа» сегодня нет.**

Проверено на четырёх проектах: Camoufox (Firefox, нет UI и команд), ShardX
(закрытое ядро, нет команд), Donut Browser (закрытое ядро Wayfern, нет команд),
fingerprint-chromium (открытые патчи, но нет ни UI, ни модели персон, ни команд).
**Ни один не закрывает командную работу.**

Перепроверено 14.08.2026 с добавлением мелких проектов (OpenIncognito,
`brodev3/antidetect`) — исключений не появилось. Пятый заход, ни одного
многопользовательского OSS.

- Camoufox закрывает «OSS + своё ядро», но это Firefox и это библиотека, а не продукт
  для команды: ни проектов, ни ролей, ни синхронизации, ни UI.
- Donut Browser закрывает «OSS + менеджер профилей», но без собственного ядра его
  антидетект-свойства ограничены.
- Всё, что закрывает командную работу, — закрытый платный SaaS.

Это и есть позиция Fury.

## Где реально можно быть лучше платных

Не «дешевле» — бесплатность сама по себе не аргумент для того, кто зарабатывает
на аккаунтах. Три содержательных преимущества:

### 1. Самохостинг и проверяемость

Cookies и пароли от боевых аккаунтов лежат на чужом сервере — это то, с чем все
мирятся, но чему никто не рад. Self-hosted + envelope encryption + открытый код,
который можно прочитать и убедиться, что сервер не может расшифровать бандл, —
аргумент, который платные не могут повторить по модели бизнеса.

### 2. Полнота покрытия векторов

Здесь была гипотеза «конкуренты не закрывают WebGPU». Она **проверена
измерением** — 30.07.2026, AdsPower 8.4.3 против настоящего Chrome 150 на одном
Mac (M5). Результат оказался и слабее, и сильнее ожидаемого.

**Что AdsPower подменяет:** строку WebGL renderer (M5 → M1 Pro), audio-хеш,
список голосов speechSynthesis, `hardwareConcurrency` (10 → 8), `deviceMemory`
(16 → 8), таймзону.

**Что оставляет нетронутым — байт в байт как у чистого Chrome:**

| Вектор | Состояние |
|---|---|
| **`canvas.toDataURL()`** | **идентичен чистому Chrome** |
| `getClientRects()` | идентичен |
| WebGL `readPixels()` | идентичен — сменена только строка renderer, не пиксели |
| Метрики шрифтов | идентичны |
| WebGPU `limits` (36 значений) | идентичны |
| `enumerateDevices()` | идентичен |

Главное здесь не WebGPU, а **canvas**. Это самый проверяемый вектор в индустрии,
и на этом профиле его отпечаток совпадает с отпечатком голой машины. Практическое
следствие: все профили AdsPower на одной машине **связываются между собой и с
настоящим Chrome пользователя по canvas-хешу**.

Оговорка: у AdsPower настройки отпечатка задаются на профиль (canvas:
шум / реальный / блок). Измерен один профиль, и это может быть его настройка,
а не дефолт продукта. Но пользователь при этом не знает, что работает без защиты —
и наш бейдж консистентности в списке профилей ([12](12-ui-reference.md)) как раз
для этого.

**По WebGPU вывод честно неполный.** AdsPower меняет `architecture`
(`metal-3` → `common-3`), но не трогает `limits`. Противоречие это или нет,
зависит от того, различаются ли `limits` у M1 Pro и M5 — а для ответа нужен
второй Mac с другим чипом. Открытый вопрос, снимается одним прогоном
`capture-chrome.sh` на любой другой машине.

Вывод для продукта: планка ниже, чем казалось. Работающий коммерческий продукт
с платящими клиентами оставляет canvas, clientRects, шрифты и WebGPU-лимиты
нетронутыми. Полное покрытие векторов из [02](02-fingerprint-surface.md) —
реальное преимущество, а не косметическое.

### 3. Публичная база детектируемости

Никто из платных не публикует результаты прогонов против CreepJS и антиботов —
все пишут «лучший отпечаток на рынке». Открытый проект может публиковать
результаты недельных прогонов ([07](07-detection-baseline.md)) как есть.
Это одновременно маркетинг и дисциплина.

## Функциональный минимум для паритета

Без этого проектом не будут пользоваться, каким бы хорошим ни было ядро:

Сверено с кодом 14.08.2026. Отмечено только то, что найдено в файлах — не в
намерениях.

- [x] Массовые операции: создание пачкой и клонирование (`BulkProfiles.tsx`,
      каждому свой seed), теги (`store.rs`)
- [ ] **Импорт профилей CSV** — CSV разбирается только для прокси
      (`shared-rs/src/proxy_list.rs`), профили не импортируются никак
- [ ] **Групповая смена прокси** — назначения прокси пачке профилей нет
- [x] Импорт cookies (`agent/src/cookies.rs`, `Cookies.tsx`) и импорт профилей
      из Chrome/Edge/Brave (`agent/src/import_browser.rs`)
- [ ] Импорт профилей из конкурентов — не сделан, и это отдельная задача от
      импорта из обычных браузеров
- [x] Local API (`agent/src/http.rs`) + примеры: `examples/playwright_example.py`,
      `puppeteer_example.mjs`, `api.sh`, `fury.py`, `rotate.py`. Selenium примера нет
- [x] Синхронизация расширений вместе с профилем (`agent/src/ext.rs`)
- [x] Быстрый поиск и фильтры (`App.tsx`, поиск + фильтр по проекту + «только
      открытые»); фильтра по статусу нет, потому что нет самого статуса — см. ниже
- [x] Заметки и хранилище кредов с TOTP — `shared-rs/src/totp.rs` (493 строки,
      RFC 6238), `Logins.tsx`, секрет запечатан vault'ом. **В
      [09](09-roadmap.md) этот пункт до 14.08.2026 стоял незакрытым — ошибка описи**
- [ ] **Проверка прокси пачкой** — `proxies.check` умеет один
      (`ipc.rs:605`), UI вызывает его из формы прокси и из редактора профиля.
      Массовой проверки списка нет
- [x] Тёмная тема (`desktop/src/theme.ts`, три состояния: system / dark / light)

Добавлено по пересмотру 14.08.2026 — есть у большинства, нет у нас:

- [ ] **Сохраняемые шаблоны профилей.** `create_profiles` принимает `template`
      разовым аргументом (`api.ts:569`), сохранённой сущности нет. У Octo это
      шаблон с автоназначением прокси, у Dolphin — готовые под Facebook Ads,
      TikTok Ads и Google Ads
- [ ] **Пользовательские статусы профиля** («в прогреве», «на бане», «готов»)
      с цветом. Колонка «Статус» в `ProfileTable.tsx` показывает состояние
      **лока**, а не стадию аккаунта; теги есть, но тег — не состояние
- [ ] **Синхронизатор окон** — см. пересмотр выше
- [ ] **Прогрев cookies** — см. пересмотр выше

## Чего сознательно не делаем

- **Свои прокси.** Это отдельный бизнес с закупкой трафика.
- **Готовые бот-сценарии под конкретные соцсети.** Ниша Dolphin, требует постоянной
  поддержки под изменения площадок, и именно она притягивает злоупотребления.
- **Облачные профили с запуском в браузере.** Парк машин и стриминг — несовместимо
  с бесплатной моделью.
- **Мобильные отпечатки в v1.** Отдельное ядро, отдельный объём работы.

## Риск, о котором стоит помнить

Открытый код антидетекта означает, что антибот-вендоры прочитают ваши патчи и напишут
детекты именно под них. Это реальный минус открытости, и его надо принимать осознанно.

Смягчение — то же, что у Camoufox: важна не секретность, а **скорость реакции**.
Детект пишется под конкретную реализацию; если вы за неделю меняете реализацию,
детект устаревает. Закрытые продукты выигрывают время, но проигрывают в скорости
исправлений, потому что у них нет внешних контрибьюторов, находящих утечки.

При этом часть патчей (например, точные значения GREASE-позиций или алгоритм шума)
можно держать в приватном репозитории и подмешивать в релизные сборки — как делает
Camoufox, оставляя основной код открытым и собираемым. Решение принимаем позже,
когда появится что защищать.

---

Источники пересмотра 12.09.2026: [AdsPower release notes](https://www.adspower.com/release), [Multilogin release notes](https://multilogin.com/release-notes/), [GoLogin release notes](https://gologin.com/release-notes/2/), [Kameleo release notes](https://kameleo.io/release-notes), [Undetectable changelog](https://undetectable.io/changelog/), [Dolphin — updates](https://dolphin-anty.com/blog/en/dolphin-anty-updates-in-the-past-three-months/), [Octo — features](https://blog.octobrowser.net/octo-browser-review-main-features), [ShardBrowser](https://github.com/ProxyShard/ShardBrowser), [CloakBrowser-Manager](https://github.com/CloakHQ/CloakBrowser-Manager), [Donut v0.30.0](https://github.com/zhom/donutbrowser/releases/tag/v0.30.0), [Camoufox releases](https://github.com/daijro/camoufox/releases), [Proxyway — Best Antidetect Browsers](https://proxyway.com/best/antidetect-browsers).

Источники по расстановке рынка: [GoLogin — Best Antidetect Browsers](https://gologin.com/blog/best-antidetect-browsers/),
[Massive — Comparison Guide](https://www.joinmassive.com/blog/best-anti-detect-browsers),
[Afina — 13 Tested](https://afina.io/en/blog/best-antidetect-browsers-comparison-2026),
[Camoufox](https://github.com/daijro/camoufox),
[Donut Browser](https://github.com/zhom/donutbrowser).
